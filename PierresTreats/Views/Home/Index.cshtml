@{
  Layout = "_Layout";
}
@using PierresTreats.Models;

<h1>Welcome back Pierre!</h1>

<h4>Here is a list of all of your treats and flavors.</h4>

<div>
  <h6>Treats</h6>
  @if(ViewBag.Treats.Count == 0)
  {
    <p>No treats yet entered.</p>
  }
  <ul>
  @foreach(Treat treat in ViewBag.Treats)
  {
    <li>@Html.ActionLink($"{treat.Name}" , "Details" , "Treats", new { treatId = treat.TreatId })</li>
  }
  </ul>
</div>

<div>
  <h6>Flavors</h6>
  @if(ViewBag.Flavors.Count == 0)
  {
    <p>No flavors yet entered.</p>
  }
  <ul>
  @foreach(Flavor flavor in ViewBag.Flavors)
  {
    <li>@Html.ActionLink($"{flavor.Type}" , "Details" , "Flavors", new { flavorId = flavor.FlavorId })</li>
  }
  </ul>
</div>

<p>@Html.ActionLink("See all treats", "Index", "Treats")</p>
<p>@Html.ActionLink("See all flavors", "Index", "Flavors")</p>

<p>@Html.ActionLink("Create an account", "Index", "Account")</p>