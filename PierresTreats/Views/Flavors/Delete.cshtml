@{
  Layout = "_Layout";
}

@model PierresTreats.Models.Flavor

<h2>Are you sure you want to delete this flavor?</h2>

@Html.DisplayNameFor(model => model.Type): @Html.DisplayFor(model => model.Type)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}

@Html.ActionLink("Back to all flavors", "Index")
@Html.ActionLink($"Back to {Model.Type} details" , "Details" , new { flavorId = Model.FlavorId })